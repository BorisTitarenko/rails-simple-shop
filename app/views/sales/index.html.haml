.card-columns
  - @current_sales.each do |ins|
    .card
      .card-body.p-1
        = image_tag ins.image.variant(resize: "400x400"), class: "d-block h-100 w-100"
        = link_to ins.brand + " " + ins.model, instrument_path(ins)
        %br
          = number_to_currency(ins.price)
        = link_to "Submit", sale_path(ins), method: :put, class: "btn btn-success mr-5 mt-5"
        = link_to "Decline", sale_path(ins), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger ml-5 mt-5"